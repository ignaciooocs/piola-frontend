import './Loading.css'

const Loading = ({ className }) => {
  return (
    <div className={className}>
      {/* <div className={loader} /> */}
      <div className='loader' />
    </div>
  )
}

export default Loading
